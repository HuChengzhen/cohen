server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql:///cohen?serverTimezone=GMT%2B8&characterEncoding=UTF-8
    username: root
    password: ASD2100150

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations:
    - classpath:/mapper/*.xml

logging:
  level:
    com.huchengzhen: debug